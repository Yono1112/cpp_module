#ifndef SPAN_HPP
#define SPAN_HPP

#include <iostream>

class Span {
	public:
		Span();	
		Span(unsigned int n);	
		Span(const Span& other);	
		~Span();
		// void	addNumber(unsigned int n);
		// unsigned int	shortestSpan();
		// unsigned int	longestSpan();
	private:
};

#endif
